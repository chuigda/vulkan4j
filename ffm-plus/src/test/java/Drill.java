public class Drill {
    public static void main(String[] args) {
//        int bits = 0b11110000_10101010_10101010_00001111;
//
//        System.out.println("bits: " + Integer.toBinaryString(bits));
//
//        System.out.println("readBits( 0, 32) = " + Integer.toBinaryString(BitfieldRead.readBits(bits, 0, 32)));
//        System.out.println("readBits( 2,  8) = " + Integer.toBinaryString(BitfieldRead.readBits(bits, 2, 8)));
//        System.out.println("readBits( 0,  8) = " + Integer.toBinaryString(BitfieldRead.readBits(bits, 0, 8)));
//        System.out.println("readBits( 0, 24) = " + Integer.toBinaryString(BitfieldRead.readBits(bits, 0, 24)));
//        System.out.println("readBits( 8, 24) = " + Integer.toBinaryString(BitfieldRead.readBits(bits, 8, 24)));
//        System.out.println("readBits(24, 32) = " + Integer.toBinaryString(BitfieldRead.readBits(bits, 24, 32)));
//        System.out.println("readBits( 0,  1) = " + Integer.toBinaryString(BitfieldRead.readBits(bits, 0, 1)));
//        System.out.println("readBits( 4,  5) = " + Integer.toBinaryString(BitfieldRead.readBits(bits, 4, 5)));
//        System.out.println("readBits( 0,  0) = " + Integer.toBinaryString(BitfieldRead.readBits(bits, 4, 4)));
    }
}
